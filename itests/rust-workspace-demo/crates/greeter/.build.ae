import build
import rust
import rust (crate_name, edition, ws_dep)

main() {
    b = build.start()
    build.dep(b, ".build.ae")
    rust.cargo_crate(b) {
        crate_name("greeter")
        edition("2021")
        ws_dep("serde")
    }
}
