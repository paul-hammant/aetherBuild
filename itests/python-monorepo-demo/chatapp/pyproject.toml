[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "chatapp"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "setuptools>=70,<81",
]

[tool.setuptools.packages.find]
where = [".."]
include = ["chatapp*"]
