import build
import python
import python (pip)

main() {
    b = build.start()
    python.install(b) {
        pip("setuptools>=70,<81")
        pip("pytest")
    }
    python.lint(b)
    python.typecheck(b)
}
