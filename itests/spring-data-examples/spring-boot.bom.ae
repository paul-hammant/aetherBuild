// Spring Boot BOM — parsed as text by aeb-resolve.jar
// Lines with maven_bom("g:a:v") and maven_repo("url") are extracted by the resolver.
maven_bom("org.springframework.boot:spring-boot-dependencies:4.0.4")
