import build
import build (dep, load_deps_file)
import ts

test_libs_cart_cart_H_page(s: ptr) {
    b = build.begin(s, "test:libs/cart/cart-page")
    if b == 0 { return 0 }
    dep(b, "libs/cart/cart-page")
    load_deps_file(b, "../../../react.deps.ae")
    ts.jest_project(b)
}
