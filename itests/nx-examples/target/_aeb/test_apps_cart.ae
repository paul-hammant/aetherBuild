import build
import build (dep, load_deps_file)
import ts

test_apps_cart(s: ptr) {
    b = build.begin(s, "test:apps/cart")
    if b == 0 { return 0 }
    dep(b, "apps/cart")
    load_deps_file(b, "../../react.deps.ae")
    ts.jest_project(b)
}
