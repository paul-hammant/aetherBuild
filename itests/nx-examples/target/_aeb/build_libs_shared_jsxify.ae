import build
import build (dep, load_deps_file)
import ts
import ts (skip_lib_check)

build_libs_shared_jsxify(s: ptr) {
    b = build.begin(s, "libs/shared/jsxify")
    if b == 0 { return 0 }
    load_deps_file(b, "../../../base.deps.ae")
    ts.tsc(b) {
        skip_lib_check()
    }
}
