import build
import build (dep, load_deps_file)
import ts

test_apps_products(s: ptr) {
    b = build.begin(s, "test:apps/products")
    if b == 0 { return 0 }
    dep(b, "apps/products")
    load_deps_file(b, "../../angular.deps.ae")
    ts.jest_project(b)
}
