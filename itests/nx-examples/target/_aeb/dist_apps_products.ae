import build
import build (dep, load_deps_file)
import ts

dist_apps_products(s: ptr) {
    b = build.begin(s, "dist:apps/products")
    if b == 0 { return 0 }
    dep(b, "apps/products")
    load_deps_file(b, "../../angular.deps.ae")
    ts.ng_build(b)
}
