import build
import build (dep, load_deps_file)
import ts

dist_apps_cart(s: ptr) {
    b = build.begin(s, "dist:apps/cart")
    if b == 0 { return 0 }
    dep(b, "apps/cart")
    load_deps_file(b, "../../react.deps.ae")
    ts.webpack_bundle(b)
}
