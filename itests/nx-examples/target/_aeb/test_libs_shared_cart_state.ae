import build
import build (dep, load_deps_file)
import ts

test_libs_shared_cart_state(s: ptr) {
    b = build.begin(s, "test:libs/shared/cart/state")
    if b == 0 { return 0 }
    dep(b, "libs/shared/cart/state")
    load_deps_file(b, "../../../../angular.deps.ae")
    ts.jest_project(b)
}
