import build
import build (dep, load_deps_file)
import ts

test_libs_shared_header(s: ptr) {
    b = build.begin(s, "test:libs/shared/header")
    if b == 0 { return 0 }
    dep(b, "libs/shared/header")
    load_deps_file(b, "../../../base.deps.ae")
    ts.jest_project(b)
}
