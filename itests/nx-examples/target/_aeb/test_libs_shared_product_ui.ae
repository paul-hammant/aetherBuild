import build
import build (dep, load_deps_file)
import ts

test_libs_shared_product_ui(s: ptr) {
    b = build.begin(s, "test:libs/shared/product/ui")
    if b == 0 { return 0 }
    dep(b, "libs/shared/product/ui")
    load_deps_file(b, "../../../../base.deps.ae")
    ts.jest_project(b)
}
