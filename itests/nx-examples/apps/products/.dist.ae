import build
import build (dep, load_third_party_deps)
import ts

main() {
    b = build.start()
    dep(b, "apps/products")
    load_third_party_deps(b, "../../angular.deps.ae")
    ts.ng_build(b)
}
